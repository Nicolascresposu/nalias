[package]
name = "nalias-lite"
version = "0.1.0"
edition = "2024"
description = "Minimal direct-wrapper command aliases for Windows"
license = "MIT"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_Registry",
    "Win32_UI_WindowsAndMessaging",
] }

